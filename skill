skills_list = ["python", "java", "react", "sql", "machine learning"]

def extract_skills(text):
    found = []
    for skill in skills_list:
        if skill.lower() in text.lower():
            found.append(skill)
    return found
